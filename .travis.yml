language: java
sudo: required
jdk:
  - oraclejdk8
notifications:
  email: false
before_install:
 - chmod +x gradlew
install: true
script:
 - sudo apt-get update && sudo apt-get install oracle-java8-installer
 - java -version